#include "stdafx.h"
#include "chap7.h"
using namespace std;

int main() {
    TEST(testQuadratic);
    TEST(testAltQuadratic);
    TEST(testMean);
    TEST(testStandardDeviation);
    TEST(testMin);
    TEST(testMax);
    TEST(testRanduniform);
    TEST(testNormal);
    TEST(testBoxMuller);
    return 0;
}
